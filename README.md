# Structures and Algorithms in C++

A collection of data structures and algorithms implemented in modern C++.

## Project layout

- `include/saa/` — public headers
- `tests/` — correctness tests, run through CTest
- `examples/` — small programs demonstrating use
- `benchmarks/` — optional performance measurements